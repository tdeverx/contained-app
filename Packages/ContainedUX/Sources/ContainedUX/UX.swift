import SwiftUI

/// Root namespace for reusable interaction, placement, and morphing systems.
public enum UX {}

public extension UX {
    enum Toolbar {}
    enum Panel {}
    enum Morph {}
    enum SafeArea {}
    enum Measurement {}
}
