- Split runtime contracts into `ContainedRuntime` and the current Apple `container` adapter into `AppleContainerRuntime`, with open-ended runtime identifiers, typed stats streams, runtime-neutral create/import models, adapter-owned Compose/image-default translation, and disabled per-container core selection so future Docker-compatible or other engine adapters can be added as sibling modules.
